# Free key from https://www.football-data.org/client/register
# When set, the WC26 match center pulls live scores/tables/scorers automatically.
# When missing, the site runs on the built-in snapshot data (lib/snapshot.ts).
FOOTBALL_DATA_API_KEY=
